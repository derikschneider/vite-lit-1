// src/main.ts

// Import your component so Vite includes it in the build
import './my-element';

// Import global CSS if you have any
import './index.css';

// Optional: attach an instance of your component to the DOM
// (only needed if your component isn’t already self-rendering)
// const root = document.createElement('div');
// root.innerHTML = `<my-element></my-element>`;
// document.body.appendChild(root);